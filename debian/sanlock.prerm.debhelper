# Automatically added by dh_installinit
if [ -x "/etc/init.d/sanlock" ] && [ "$1" = remove ]; then
	invoke-rc.d sanlock stop || exit $?
fi
# End automatically added section
# Automatically added by dh_installinit
if [ -x "/etc/init.d/wdmd" ] && [ "$1" = remove ]; then
	invoke-rc.d wdmd stop || exit $?
fi
# End automatically added section
